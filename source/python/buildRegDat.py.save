#!/usr/bin/env python
# coding: utf-8

from __future__ import division, print_function
import os
import sys
import datetime

def getnxny(inputdir,outputdir,SHARP)
    input = open(inputdir+'/'+'bx_'+SHARP+'_0.txt',r)
    xlength = file.readline()
    nx = len(xlength.split())
   
    counter = 1 # we have already read one line in
    content = file.read()
    CoList = content.split("\n")
   
    for i in CoList:
        if :
           counter += 1

    ny = counter

    return nx,ny

 
