import create_timeseries
import plotting
import numpy as np

'''
^^^^^^^^^^
* Inputs *
^^^^^^^^^^
'''
regionName='956'
filenumber = 10
cutoff = 50
v_smoothing = 20                          # velocity sampling
sampling = 1
dt = 720                                 # time interval 


# plot a map of a variable

title = ' winding'
path= '/home/dmac/test_codes/Data'

path, filename = plotting.name_file(path,regionName,cuttoff,V_smoothing,sampling,filenumber)

nx, ny = plotting.getnxny(path)

variables = plotting.read_data(path,filename)
sorted_variables = plotting.sort(variables,'X1')

bz = plotting.convert_array(sorted_variables['bz'],nx,ny)

plotting.plotmap(bz,nx,ny,'bz','Bz',path,filename,save=False)


